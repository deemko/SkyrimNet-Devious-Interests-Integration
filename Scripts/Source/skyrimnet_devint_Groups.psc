ScriptName skyrimnet_devint_Groups extends Quest

; Placeholder script attached to the same Quest as skyrimnet_devint_MCM, purely so
; that script can hold a self-reference to this Quest instance (matches the pattern
; used by SkyrimNet_UDNG's own Groups/MCM pair). No logic needed for v1.
